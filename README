mikey.bike
----------

  If only there were a .bikey TLD.


BUILDING

  requires cabal. then run:

  $ make build


DEVELOPMENT

  run a local server on port 8000 (standard hakyll site setup):

  $ make preview


DEPLOY

  bin/deploy: a simple rsync up to my server
